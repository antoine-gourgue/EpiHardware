# Prisma CLI (migrate / db push / seed / studio) reads DATABASE_URL from here.
# Copy to packages/db/.env when using PostgreSQL. Matches docker-compose.yml.
DATABASE_URL="postgresql://epihardware:epihardware@localhost:5432/epihardware?schema=public"
